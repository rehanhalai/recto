/**
 * Feed feature types — re-exports shared types from @recto/types
 * and keeps feed type imports centralized.
 */

export type {
  ApiEnvelope,
  PostWithRelations,
  PaginatedResponse,
} from "@recto/types";
